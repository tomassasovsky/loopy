name: update_repository
description: >-
  Platform-agnostic software-update domain: the update manifest, a pluggable
  platform backend (appliance RAUC / desktop Sparkle), and the check/stage/apply
  orchestration the app's UpdateCubit drives.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
