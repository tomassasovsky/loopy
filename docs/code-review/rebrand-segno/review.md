# Code Review — rebrand/segno (Loopy → Segno)

Local working copy (not committed — spell-check gates `**/*.md`).

0 Critical / 0 Important open after post-review fixes. See PR #497.
Suggestions only: OTA channel normalize, manifest basename harden, `/data`
migrate helper, optional `le_`/`lpw_` rename, console golden refresh, stale
local `loopy_engine_test.dylib`.
