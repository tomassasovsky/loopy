/// The unified Signal surface: three side-by-side lists (inputs → tracks →
/// outputs) with output-hued routing chips and tap-to-trace, replacing the old
/// node-and-wire graph. Exports only the public entry point.
library;

export 'signal_list_view.dart' show showSignalPage;
